# Traceability: Materialize Autopilot Task Ledgers

| Requirement | Design Source | Task Coverage | Validation Evidence |
| --- | --- | --- | --- |
| Explicit starts materialize selected changes | Trigger Model; Preflight; Publication Algorithm | Plan And Contract; Ledger Builder; Plugin-Owned Publication; Runtime Integration | `node tools/test-autopilot-ledger-materialization.ts`; `node tools/test-autopilot-contract.ts`; `npm test`; generated-ledger `npm run autopilot:validate -- <materialized-ledger>` inside focused test |
| Read-only paths stay read-only | Trigger Model; Interaction With Existing Flows | Plan And Contract; Runtime Integration | Read-only safety tests in `tools/test-autopilot-ledger-materialization.ts`; cheap-check snapshot in `tools/test-autopilot-check.ts`; trigger policy tests in `tools/test-autopilot-programmatic-triggers.ts`; `npm test` |
| Generated ledgers are valid before publication | Ledger Shape; Publication Algorithm | Ledger Builder; Plugin-Owned Publication | `validateTaskLedger` candidate/read-back/final validation; no-clobber/publish-failure cleanup tests; `node tools/test-autopilot-ledger-materialization.ts`; `npm run validate`; `npm test` |
| Materialization output is machine-readable | Output Contract | Plan And Contract; Runtime Integration; Documentation And Instructions | `ledger_materialized` contract assertions in `tools/test-autopilot-ledger-materialization.ts`, `tools/test-autopilot-contract.ts`, and `tools/test-autopilot-instruction-drift.ts`; code-quality and test-coverage reviewer gates clean |
| Plain `/autopilot` does not require `<change-id>` | Trigger Model; Preflight; Risks | Plan And Contract; Runtime Integration | Unscoped selected-primary materialization test and prompt-resolved `changeId` test in `tools/test-autopilot-ledger-materialization.ts`; default/custom `ledgerRoot` symlink-junction no-write tests |
| Documentation explains who creates `task.json` | Design Principles; Rollout | Documentation And Instructions | README/skill/command drift tests in `tools/test-autopilot-instruction-drift.ts`; `instruction-artifact-reviewer`; `npm run validate`; `npm test` |
