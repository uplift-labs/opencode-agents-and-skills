# Tasks: Enable Autopilot Worker Dispatch

## Tests First

- [x] Add runtime-store tests for schema validation, atomic save/load behavior, corrupt or missing state recovery, active-run evidence persistence, and consumed report id persistence. Evidence: `tools/test-autopilot-runtime-store.ts` covers strict allowed-key schema validation, optional `null` rejection, unknown-field rejection before normalization, missing/corrupt/invalid persisted-state recovery, stable cloned in-memory save/load, in-memory rollback, full active-run round trip, consumed report id sorting/dedup, invalid-save rollback, forced rename-failure cleanup/rollback, same-store recovery after failure, and detached overlapping save serialization. Validation passed: `node tools/test-autopilot-runtime-store.ts`, `npm test`, `npm run validate` (existing warning: top-level `allow` in `opencode.json`), `openspec validate --all`, and `npm run autopilot:check -- --level standard --change enable-autopilot-worker-dispatch` (exit 0 with advisory freshness unknowns).
- [x] Add report-parser tests covering one complete valid `AUTOPILOT_WORKER_REPORT` envelope and negative cases for missing, partial, duplicate, invalid JSON, unknown report id, and mismatched run/task/session/status evidence. Evidence: `tools/test-autopilot-worker-report-parser.ts` covers a complete standalone marker-line plus JSON payload, optional target status behavior, non-empty blocker and nested evidence preservation, missing stored session evidence, missing marker, partial marker, partial-plus-complete marker, split-line and inline marker rejection, duplicate complete markers, invalid JSON, unknown marker report id, already-consumed duplicate report id, mismatched payload identity/status evidence, payload report id mismatch, invalid payload shape, invalid validation status, invalid MR status, and unknown fields. Validation passed: `node tools/test-autopilot-worker-report-parser.ts`, `npm test`, `npm run validate` (existing warning: top-level `allow` in `opencode.json`), and `openspec validate --all`.
- [x] Add ledger-transition-writer tests using temp OpenSpec repositories for valid protected ledger mutation, invalid next-ledger rollback, stale revision rejection, duplicate report id idempotency, and post-write validation. Evidence: `tools/test-autopilot-ledger-transition-writer.ts` covers valid temp-repo protected ledger mutation, post-write validation, stale revision number/content hash rejection with byte preservation, unsafe path rejection, symlinked `automation` path rejection, invalid next-ledger rollback with temp cleanup, wrong current ledger id rejection, stale current status rejection, invalid current ledger rejection, blocked-ledger resolution clearing stale blockers, duplicate report id idempotency with exact byte preservation, duplicate report id with mismatched history evidence rejection, and mismatched report evidence rejection. Validation passed: `node tools/test-autopilot-ledger-transition-writer.ts`, `npm test`, `npm run validate` (existing warning: top-level `allow` in `opencode.json`), and `openspec validate --all`.
- [x] Add phase-dispatcher tests for `Ready`, `Analyze`, `Implementation`, `Review`, `Acceptance`, `Blocked`, and terminal statuses across representative task types. Evidence: `tools/test-autopilot-phase-dispatcher.ts` covers Ready feature Analyze-first, Ready typo direct minimal implementation, Ready feature `autoMinimalAnalyze` using `phaseEvidence.analyze`, Analyze feature Implementation, Analyze bugfix Implementation with validator-owned type gate fields, Analyze planning no-implementation Review, Implementation Review type gates for tooling/config/performance/protocol, Review Acceptance reviewer evidence, all MR-wait statuses, Acceptance feature/research Done verification, Blocked blocker preservation, and Done/Failed/Cancelled terminal decisions. Validation passed: `node tools/test-autopilot-phase-dispatcher.ts`, `npm test`, `npm run validate` (existing warning: top-level `allow` in `opencode.json`), `npm run openspec:validate`, and `npm run autopilot:check -- --level standard --change enable-autopilot-worker-dispatch` (exit 0 with advisory freshness unknowns).
- [x] Add worker-prompt-builder tests proving prompts include phase goal, task type, scope read/write/forbidden boundaries, protected-path prohibition, validation expectations, and strict report contract. Evidence: `tools/test-autopilot-worker-prompt-builder.ts` covers phase goal/task identity, scope boundaries/protected-path prohibition, validation expectations, and strict report marker/payload instructions.
- [x] Add fake worker-session adapter tests proving `autopilot_run_next` creates one child worker session only when capability is available and dispatch is explicitly enabled. Evidence: `tools/test-autopilot-worker-session-adapter.ts` covers SDK-shaped create/prompt/messages calls, worktree directory preference, missing capability, create/prompt/messages thrown errors, prompt/user message filtering, and fail-closed rejection of role-less prompt-shaped report markers.
- [x] Add controller integration tests proving enabled serial dispatch returns `advanced` with `tasksStarted[]`, active status evidence, and no additional claim while a serial worker is active. Evidence: `tools/test-openspec-autopilot-controller-worker-dispatch.ts` covers enabled dispatch, compact active status output without prompt/report leakage, same-instance and restart/reload duplicate-claim suppression, dependency-blocked no-dispatch, blocked/MR-wait active ownership, and stop behavior.
- [x] Add controller integration tests proving disabled or unavailable worker dispatch preserves `ready_runtime_deferred` or a specific blocker without protected ledger mutation. Evidence: disabled, unavailable capability, create/prompt failure, runtime recovery conflict, and corrupt stop conflict cases are covered in `tools/test-openspec-autopilot-controller-worker-dispatch.ts`.
- [x] Add `autopilot_collect` integration tests proving complete matching reports advance the protected ledger, repeated reports are idempotent, malformed reports block, and stale reports return `runtime_evidence_conflict`. Evidence: controller tests cover valid collect, overlapping collect serialization, repeated consumed collect deferral, malformed report conflict, stale ledger writer conflict with byte preservation/report-id not consumed/running-state restoration, read exception restoration, blocked/MR-wait reports, and phase continuation; parser/writer tests cover stale/mismatched/duplicate evidence.
- [x] Add protected-path and scope-guard tests for worker-originated patch/edit/write/bash paths including Windows separators, absolute paths, traversal, `scope.forbidden`, and protected Autopilot paths. Evidence: `tools/test-autopilot-protected-path-guard.ts` covers shared protected patterns, apply_patch, direct edit/write tools, shell aliases, compound/cwd-relative shell bypasses, assigned scope, forbidden/protected/absolute/traversal paths, safe validation command allowlisting, compound shell fail-closed classification, and fail-closed shell classification; plugin smoke covers source-equivalent worker hook enforcement for running, stopped, failed, done, out-of-scope, and corrupt-runtime cases.
- [x] Add event/scheduler integration tests proving owned idle worker report evidence schedules exactly one scoped collect, while passive or unrelated events cannot claim work. Evidence: `tools/test-autopilot-programmatic-triggers.ts`, `tools/test-autopilot-bundle-smoke.ts`, and `tools/test-autopilot-plugin-worker-dispatch-smoke.ts` cover owned worker idle collect, marker-before-idle behavior, repeated idle no double advance, passive no-run-next, runtime-owned revalidation, and raw report/log redaction.
- [x] Add source-equivalent plugin adapter tests with fake OpenCode client/session APIs and clear capability-missing behavior when APIs are unavailable. Evidence: `tools/test-autopilot-plugin-worker-dispatch-smoke.ts` covers fake SDK-shaped live dispatch/collect, durable stopped/failed/done scope blocking, and log redaction; adapter/controller tests cover missing APIs and structured capability failures.

## Implementation

- [x] Add `AutopilotRuntimeStore` interfaces plus in-memory and durable implementations with stable JSON schema, atomic writes, validation, and recovery behavior. Evidence: `tools/autopilot-runtime-store.ts` defines runtime snapshot/run records, strict runtime validation before normalization, whitelisted normalization, in-memory store, durable file store, corrupt/missing/invalid-state recovery, temp-file atomic save, and per-store-instance write serialization. Reviewer evidence: `code-quality-reviewer` final re-review reported no blocking findings after cleanup; `test-coverage-reviewer` final re-review was clean and independently reran the focused runtime-store test.
- [x] Extend `AutopilotOptions` and plugin option parsing with an explicit safe default such as `workerDispatch.enabled: false`. Evidence: `AutopilotOptions.workerDispatch` is optional, plugin creates durable runtime/session adapter only when `workerDispatch.enabled === true`, and disabled tests preserve `ready_runtime_deferred`.
- [x] Refactor existing in-memory claim/collect helpers so live runtime can obtain a validated next-ledger object and transition evidence without duplicating validator logic. Evidence: live collect uses `applyAutopilotLedgerTransition`, shared ledger validation, and phase dispatcher decisions rather than duplicating validator logic.
- [x] Implement `LedgerTransitionWriter` that performs read-current, validate-current, verify revision/status/path, apply transition, validate-next, atomic write, and post-write validation. Evidence: `tools/autopilot-ledger-transition-writer.ts` provides active OpenSpec ledger path safety with symlink/junction guard, current ledger validation, stored run/report evidence checks, stale revision/status detection, exact duplicate report id idempotency checks, next-ledger construction with history/revision/MR/blocker updates, shared next-ledger validation, temp-file write/rename, optimistic pre-rename revision freshness check, and post-write validation. Reviewer evidence: `code-quality-reviewer` final recheck was clean after symlink hardening; `test-coverage-reviewer` final recheck was clean and independently reran the focused writer test.
- [x] Implement strict worker report parser and typed report validation against stored run, worker, session, task, ledger path, status, and report id evidence. Evidence: `tools/autopilot-worker-report-parser.ts` parses exactly one standalone `AUTOPILOT_WORKER_REPORT <reportId> COMPLETE` marker and JSON payload, validates the marker before trusting payload, rejects consumed report ids, enforces schema/version/allowed keys/status values, validates required evidence, compares run/worker/session/task/ledger/status/report-id evidence against `AutopilotRunRecord`, and returns typed parsed reports or structured rejection reason codes without ledger mutation. Reviewer evidence: `code-quality-reviewer` final parser re-review was clean; `test-coverage-reviewer` final review found no blockers, independently reran the focused parser test, and its non-blocking blocker/evidence coverage gap was closed by additional tests.
- [x] Implement phase-aware dispatch policy for `Ready`, `Analyze`, `Implementation`, `Review`, and `Acceptance`, preserving terminal, blocker, and MR-wait stops. Evidence: `tools/autopilot-phase-dispatcher.ts` adds a pure `resolveAutopilotPhaseDispatch` policy that returns deterministic `dispatch`, `wait`, `blocked`, or `terminal` decisions with task type, from/to status, phase, worker goal, evidence requirements, minimal-analyze evidence, MR-wait stops, blocker evidence, and terminal no-actionable stops. Controller and worker-session wiring remain tracked separately.
- [x] Implement worker prompt builder from task ledger fields and phase policy, including explicit report-envelope and protected-path instructions. Evidence: `tools/autopilot-worker-prompt-builder.ts` and prompt builder tests.
- [x] Implement OpenCode worker-session adapter behind a small interface; keep raw session/workspace APIs out of model-facing tools. Evidence: `tools/autopilot-worker-session-adapter.ts` exposes capability/create/prompt/dispatch/read methods and plugin-only wiring; raw SDK details and explicit assistant-message report filtering are covered by adapter tests.
- [x] Update `createAutopilotController` so `runNext`, `status`, `collect`, and `stop` use durable runtime services when dispatch is enabled and fall back safely when disabled/unavailable. Evidence: controller tests cover dispatch, status with active and stopped `recentRuns[]` evidence, collect, stop, disabled/unavailable capability byte preservation, runtime recovery, stale ledger collect conflict, corrupt stop, and prompt/read exceptions.
- [x] Ensure active serial runtime state prevents duplicate claims and produces safe next actions for collect, status, stop, wait, blocker, or MR states. Evidence: duplicate, restart/reload, blocked, MR-wait, stop, and active status tests.
- [x] Implement worker-origin permission/scope/protected-path guard in plugin hook or equivalent OpenCode permission surface. Evidence: `guardAutopilotWorkerScopeToolCall` and plugin hook tests block out-of-scope, stopped/done/failed terminal-session, and corrupt-runtime worker writes while allowing simple validation commands without widening write scope.
- [x] Wire controlled worker-idle/report-marker event handling to existing scheduler/controller paths using durable worker session evidence. Evidence: source-equivalent plugin worker dispatch smoke and existing trigger tests.
- [x] Keep passive observe-mode events read-only and claim-disabled. Evidence: bundle smoke and programmatic trigger tests cover passive file/tool events scheduling status/check only, not run-next.
- [x] Add compact structured plugin logs for dispatch, collect, conflict, stop, and capability-missing events without logging raw prompts, secrets, or full report payloads. Evidence: plugin scheduler logs summarize outcome/reason/task ids/counts and tests assert source-equivalent trigger completion without raw prompt, report marker, report prose, or report JSON payload leakage.

## Documentation And Routing

- [x] Update `README.md` Autopilot bundle/runtime guidance with worker dispatch prerequisites, default disabled policy, restart requirements, and safe fallback states. Evidence: README documents SDK-shaped session calls, default disabled fallback, corrupt runtime diagnostics, single-instance boundary, restart guidance, and source-equivalent smoke commands.
- [x] Update the README routing map so explicit `/autopilot` with enabled worker dispatch is distinguished from `ready_runtime_deferred`, `active_change_handoff`, and manual `openspec-apply-change` paths. Evidence: README routing/catalog bullets document live `advanced` dispatch, `tasksStarted[]`, `plugin-owned-protected-ledger`, disabled/unavailable `ready_runtime_deferred`, and `active_change_handoff` to `openspec-apply-change`.
- [x] Update `openspec-autopilot` skill guidance so agents know when `tasksStarted[]`/`tasksAdvanced[]` prove real dispatch or ledger mutation and when `ready_runtime_deferred` still means manual handoff. Evidence: `.opencode/skills/openspec-autopilot/SKILL.md` documents worker dispatch, collect mutation, runtime-only mutation, single-instance boundary, and no-progress escape hatches.
- [x] Update drift/contract tests for any new reason codes, output fields, options, or wording introduced by live dispatch. Evidence: `tools/test-autopilot-instruction-drift.ts` asserts live dispatch/collect/worker-scope/plugin description wording, single-runtime boundary, status-only/free-form prompt `autopilot_status` boundary, enabled+capability safe-deferred tool wording, and `npm test` passed.
- [x] Document worker report format, protected-path boundaries, runtime-store ownership, and recovery/stop behavior. Evidence: README, skill, design, and traceability now document report format, protected worker scope, runtime ownership, recovery conflicts, stop behavior, and source-equivalent/live boundaries.
- [x] Review relevant artifact frontmatter, plugin descriptions, command wording, and install guidance for discoverability after runtime behavior changes. Evidence: plugin tool descriptions updated for worker dispatch/status/collect; README/skill drift tests passed.

## Review Gates

- [x] Run `code-quality-reviewer` for runtime store, ledger writer, controller changes, report parser, plugin adapter, and guard code. Evidence: first pass found role-less report parsing, stale terminal worker scope, and validation-command guard blockers; fixes added explicit assistant-only report extraction, inactive worker write blocking, validation command allowlisting, shell-control fail-closed handling, and focused regressions. Recheck reported no blocking/material findings; residual non-blocking risks are non-array SDK message fallback and validation command trust semantics.
- [x] Run `test-coverage-reviewer` for dispatch, collect, stale evidence, protected path, event scheduling, and disabled/unavailable capability paths. Evidence: first pass requested high-level stale collect, stopped/blocked status, byte preservation, and log redaction tests; focused regressions were added. Recheck verdict was clean with no blocking missing tests.
- [x] Run `instruction-artifact-reviewer` after README, skill, command, or routing wording changes. Evidence: recheck found only minor adjacent reusable routing drift; `.opencode/skills/adaptive-delivery/SKILL.md`, `instructions/global-opencode-agent-instructions.md`, `instructions/reusable-project-agent-instructions.md`, and `templates/project/AGENTS.md` were aligned, `node tools/test-autopilot-instruction-drift.ts` passed, and final targeted recheck reported no blocking findings.
- [x] Run `deployment-config-reviewer` if plugin options, package/install guidance, runtime store location, or OpenCode config shape changes materially. Evidence: recheck reported no blocking/material deployment findings; residual operational risks are strict plugin-option diagnostics follow-up, live restarted OpenCode E2E before target enablement, and documented single-runtime boundary.
- [x] Run `openspec-consistency-review` before implementation handoff or archive because this change affects Autopilot runtime, routing, and protected-state semantics. Evidence: recheck found no remaining worker-dispatch design blocker after spec/proposal/traceability fixes; checklist synchronization was addressed, and final targeted recheck reported clean implementation handoff with only archive-only retrospective and protected-ledger finalization residuals.

## Validation

- [x] `npm run validate`. Evidence: passed; known pre-existing warning remains `OpenCode permission config uses top-level allow` in `opencode.json`.
- [x] `npm test`. Evidence: passed directly after reviewer fixes and again inside `npm run autopilot:check -- --level prepush --change enable-autopilot-worker-dispatch`; suite includes new worker-session adapter, controller worker dispatch, plugin worker dispatch smoke, protected-path guard, bundle smoke, and instruction drift regressions.
- [x] `openspec validate --all`. Evidence: `npm run openspec:validate` passed directly and inside prepush Autopilot check with 15 items passed, 0 failed.
- [x] `npm run autopilot:check -- --level standard --change enable-autopilot-worker-dispatch`. Evidence: passed with exit 0 and warning status from non-blocking freshness advisory/Ready ledger evidence.
- [x] `npm run autopilot:check -- --level prepush` or `npm run prepush:validate` before ready-to-land handoff. Evidence: `npm run autopilot:check -- --level prepush --change enable-autopilot-worker-dispatch` exited 0 and ran repository validation, tests, OpenSpec validation, active ledger validation, and archive-strict freshness.
- [x] Source-equivalent plugin smoke: fake worker dispatch starts one session and reports active runtime state. Evidence: `node tools/test-autopilot-plugin-worker-dispatch-smoke.ts` passed with SDK-shaped `session.create`/`promptAsync`, durable runtime `running` state, and one worker session.
- [x] Source-equivalent plugin smoke: fake worker report collect updates only the temp protected ledger through plugin-owned writer. Evidence: plugin smoke passed with owned idle collect advancing a temp ledger through `tasksAdvanced[].mutation: "plugin-owned-protected-ledger"`, repeated idle no double collect, and log redaction checks.
- [x] Manual/live smoke after OpenCode restart only when worker-session APIs and safe plugin options are available; otherwise record capability-missing fallback evidence. Evidence: no restarted live enablement was attempted in this session; current plugin-owned `autopilot_status` returned valid Ready ledger evidence with `reasonCode: "ready_runtime_deferred"`, and docs record restart/single-runtime guidance before target enablement.

## Acceptance Criteria

- [x] With worker dispatch disabled or unavailable, current safe `ready_runtime_deferred` behavior remains compatible and loop-guarded. Evidence: disabled/unavailable controller tests preserve `ready_runtime_deferred`, do not start workers, and preserve ledger bytes; current plugin-owned `autopilot_status` returns `ready_runtime_deferred` for the protected Ready ledger.
- [x] With worker dispatch enabled and capability available, one valid dispatchable ledger can be claimed and started as exactly one worker session. Evidence: controller and source-equivalent plugin smoke tests verify one SDK-shaped child worker session and durable runtime state.
- [x] `autopilot_status` reports compact active runtime state for claimed/running/stopped/blocked runs without leaking raw prompts or secrets. Evidence: controller tests cover active running state, stopped `recentRuns[]` without active ownership, blocked active state, MR-wait active state, and prompt/report non-leakage; plugin smoke covers log redaction.
- [x] `autopilot_collect` accepts only complete matching worker reports and rejects malformed, duplicate, stale, or mismatched evidence without protected ledger mutation. Evidence: parser, writer, and controller collect tests cover valid, malformed, duplicate/consumed, stale ledger, mismatched evidence, read-error, and overlapping collect paths with byte-preservation/report-consumption assertions.
- [x] Legal report transitions are validated and written atomically to `automation/task.json` only by plugin-owned code. Evidence: ledger writer tests validate current/next/post-write state, stale revision/status rejection, symlink/junction rejection, temp atomic writes, and source-equivalent plugin collect mutates only temp protected ledgers through plugin-owned writer.
- [x] Non-terminal phases can continue or block through phase-aware policy instead of generic no-actionable output. Evidence: phase-dispatcher tests cover Ready/Analyze/Implementation/Review/Acceptance/Blocked/terminal states, and controller tests verify Analyze continuation after collect.
- [x] Worker-originated writes to protected Autopilot paths and out-of-scope paths are blocked by runtime enforcement, not only by prompt instructions. Evidence: protected-path guard tests and plugin smoke cover protected paths, assigned scope, forbidden scope, traversal/absolute/unclassified paths, shell control syntax, out-of-scope writes, stopped/done/failed inactive worker writes, and corrupt-runtime fail-closed behavior.
- [x] Controlled event-triggered collection uses only plugin-owned worker evidence and cannot start claim-capable work from passive or unrelated events. Evidence: programmatic trigger tests and bundle/plugin smoke cover owned idle/report collect, repeated consumed report suppression, passive observe status/check-only behavior, and autonomous run-next disabled without explicit owned prerequisites.
- [x] No automatic merge, deploy, protected-branch push, force-push, or destructive cleanup is introduced. Evidence: design non-goals, worker prompt contract, plugin/controller implementation, and tests introduce no merge/deploy/push/destructive cleanup path; stop is runtime-only in the serial MVP.

## Retrospective Before Archive

- [x] Review the completed change context, validation, reviewer gates, blockers, repeated work, wait time, token-heavy steps, and likely root causes. Evidence: `retrospective.md` reviews OpenSpec artifacts, source/test evidence, validation, reviewer gates, current `ready_runtime_deferred` Autopilot output, root causes, and live-enable boundaries.
- [x] Write `retrospective.md` with evidence, problems, root causes, improvements, and archive gate decision. Evidence: `openspec/changes/enable-autopilot-worker-dispatch/retrospective.md` created.
- [x] Create or update project-local OpenSpec follow-up changes for project-local findings. Evidence: no project-local findings remain; project-local issues were fixed in scope or recorded as archive/runtime-boundary evidence.
- [x] For reusable findings, create or update `opencode-dev-kit` OpenSpec proposals/changes only when the current repository owns them; otherwise record a local handoff and do not write cross-repo without explicit approval. Evidence: `npm run openspec:retro-followups -- enable-autopilot-worker-dispatch` created `retro-enable-autopilot-worker-dispatch-01-worker-dispatch-option-diagnostics-remain-a-reus` with proposal, tasks, and spec delta.
- [x] Run `npm run openspec:retro-followups -- enable-autopilot-worker-dispatch` when available so actionable retrospective findings create or update follow-up OpenSpec changes before archive. Evidence: command passed with one created `opencode-dev-kit` follow-up and `retrospectiveUpdated: true`; rerun after generator fix added the missing spec delta with `retrospectiveUpdated: false`.
- [x] Confirm archive is allowed only after the retro gate passes or an approved skip reason is recorded. Evidence: `npm run openspec:retro-gate -- enable-autopilot-worker-dispatch` returned `valid: true` and `archiveAllowed: true` after root-cause/spec-delta contract fixes.
