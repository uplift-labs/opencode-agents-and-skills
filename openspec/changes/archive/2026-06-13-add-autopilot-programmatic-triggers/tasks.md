# Tasks: Add Autopilot Programmatic Triggers

## Tests First

- [x] Add TypeScript tests for trigger event classification covering supported OpenSpec paths, unsupported paths, worker session events, blocker replies, permission replies, workspace/worktree events, and TUI command intent categories. Evidence: `tools/test-autopilot-programmatic-triggers.ts` covers active `tasks.md`, ledger/evidence paths, unsupported paths, owned/unknown worker idle, blocker replies, permission replies, workspace/worktree readiness, TUI status/check/run/stop intent, and autonomous run-next disabled-without-evidence cases.
- [x] Add scheduler tests for debounce, single-flight execution, cooldowns, normalized scope keys, source tags, disposal cancellation, recursion suppression, ordered due-job execution, and failure isolation. Evidence: `tools/test-autopilot-trigger-scheduler.ts` covers stable normalized job keys, debounce coalescing, cooldown suppression, single-flight joining, ordered due jobs, later due jobs still executing after earlier failure, dispose cancellation/rejection, and recursive Autopilot-triggered source suppression.
- [x] Add tests proving passive events schedule only status or cheap checks and never call `autopilot_run_next` by default. Evidence: `tools/test-autopilot-programmatic-triggers.ts` covers default passive file and post-tool behavior; `tools/test-autopilot-bundle-smoke.ts` covers source-equivalent file events and post-tool checkpoints without `run_next` jobs.
- [x] Add tests proving `triggerMode: off|observe|controlled|autonomous` gates behavior exactly as specified. Evidence: `tools/test-autopilot-programmatic-triggers.ts` covers mode parsing/defaults, off/observe/controlled/autonomous gates, explicit `runNextEvents`, missing prerequisites, and owned autonomous run-next eligibility.
- [x] Add tests proving worker `session.status: idle` schedules `autopilot_collect` only for plugin-owned worker sessions and only once per unconsumed worker report id. Evidence: `tools/test-autopilot-programmatic-triggers.ts` covers owned/unknown/consumed worker sessions; `tools/test-autopilot-bundle-smoke.ts` covers source-equivalent worker idle collection and repeated idle after cooldown with consumed report id.
- [x] Add tests proving `message.updated` and `message.part.updated` report markers do not trigger collection until the worker is idle or report completion is proven. Evidence: `tools/test-autopilot-programmatic-triggers.ts` covers busy marker ignored, idle message without marker ignored, mismatched report marker ignored, and matching complete marker with idle owned worker scheduling collect.
- [x] Add tests proving `question.replied`, `question.rejected`, and `permission.replied` affect only plugin-owned pending blocker/action state. Evidence: `tools/test-autopilot-programmatic-triggers.ts` covers owned blocker answer pass-through, rejected blocker status, permission alias handling, unknown/missing ids, and observe-mode disabled handling.
- [x] Add tests proving `tool.execute.after` schedules cheap checkpoints for `advanced` Autopilot outputs and suppresses no-progress loops. Evidence: `tools/test-autopilot-programmatic-triggers.ts` covers `ledger_materialized`, `advanced`, `tasksStarted`, path-derived scopes, selection fallback, multi-scope unscoped fallback, runtime evidence conflict status checkpoints, independent no-progress reason/loop-guard suppression, unsupported output shapes, mode gates, and non-claim-capable `check` job defaults.
- [x] Add protected-path guard tests for `apply_patch`, edit/write tools, and `bash` commands that target `.autopilot/**` or `openspec/changes/*/automation/**`. Evidence: `tools/test-autopilot-protected-path-guard.ts` covers shared protected-path contract patterns, `apply_patch`, `write`, `edit`, Serena write-style tools, unknown mutating path tools, mutating/read-only `bash`, shell control syntax, and `cwd`/`workdir` relative protected paths; `tools/test-autopilot-bundle-smoke.ts` covers source-equivalent before-hook blocking.
- [x] Add source-equivalent plugin tests for server `event`, `tool.execute.before`, and `tool.execute.after` handlers with fake OpenCode event/tool envelopes. Evidence: `tools/test-autopilot-bundle-smoke.ts` covers plugin `event` for file, worker, blocker, permission, workspace, and worktree events; `tool.execute.after` checkpoints; `tool.execute.before` protected-path blocking; and TUI command registration.
- [x] Add instruction/config drift tests if new trigger options, TUI command names, README guidance, or skill wording are documented. Evidence: `tools/test-autopilot-instruction-drift.ts` includes programmatic trigger documentation checks for trigger modes, hooks, protected-path guard, TUI commands, passive safety, controlled ownership, and restart guidance.

## Implementation

- [x] Extract or wrap the existing Autopilot public tool logic behind a shared TypeScript controller without changing current tool output behavior. Evidence: `.opencode/plugins/openspec-autopilot.ts` now delegates `autopilot_*` tools to `tools/openspec-autopilot-controller.ts`; `node tools/test-autopilot-contract.ts`, `node tools/test-openspec-autopilot-output.ts`, and `node tools/test-openspec-autopilot-control-output.ts` passed.
- [x] Add a deterministic trigger scheduler module with debounce, single-flight, cooldown, source tagging, recursion guard, safe logging summaries, and disposal support. Evidence: `tools/autopilot-trigger-scheduler.ts` adds deterministic enqueue/flush/snapshot/dispose behavior with stable keys, source tags, recursion suppression, pending/in-flight/cooldown snapshots, and focused tests passed through `npm test`.
- [x] Add trigger configuration parsing with safe defaults for `off`, `observe`, `controlled`, and `autonomous` modes. Evidence: `parseAutopilotTriggerOptions` in `tools/autopilot-programmatic-triggers.ts` defaults to observe mode, enables read-only observe helpers, keeps TUI/run-next opt-in, and rejects invalid mode/timing values to safe defaults.
- [x] Implement observe-mode `event` hook handling for `file.watcher.updated` on `tasks.md`, `automation/**`, reports, and retrospectives. Evidence: `.opencode/plugins/openspec-autopilot.ts` wires `event` to `classifyAutopilotEvent`; focused tests cover tasks, protected automation paths, retrospective, and report paths.
- [x] Wire observe-mode file triggers to `autopilot_status` or `autopilot:check --level cheap` when the check helper exists, without protected writes. Evidence: plugin scheduler executes status through the shared controller and cheap checks through `runAutopilotCheck`; source-equivalent smoke confirms logged status/check jobs without `autopilot_run_next`.
- [x] Implement `tool.execute.after` checkpoints for `autopilot_run_next` and `autopilot_collect` advanced/conflict outputs. Evidence: `.opencode/plugins/openspec-autopilot.ts` wires `tool.execute.after`; classifier tests cover advanced, ledger_materialized, runtime_evidence_conflict, no-progress suppression, and unsupported shapes.
- [x] Implement controlled worker idle/report-marker collection using plugin-owned runtime session/task/report evidence. Evidence: classifier and source-equivalent tests cover owned worker idle collection, report markers, consumed report id suppression, unknown sessions, runtime-state consumed report mapping, and execute-time stale-runtime suppression before controller dispatch.
- [x] Implement controlled blocker question and permission reply handling using plugin-owned request/action evidence. Evidence: classifier and source-equivalent tests cover `question.replied`, `question.rejected`, `permission.replied`, docs-shaped `answers`, option action inference, owned request ids, and unknown request ignores.
- [x] Implement workspace/worktree ready/failed handling only for plugin-owned runtime waits. Evidence: classifier and source-equivalent tests cover owned workspace/worktree ready/failed status/stop jobs and unknown workspace/worktree ignores.
- [x] Implement protected-path write guard through `tool.execute.before` or an equivalent permission-backed enforcement path. Evidence: `tools/autopilot-protected-path-guard.ts` plus plugin `tool.execute.before` block direct protected writes from `apply_patch`, edit/write tools, Serena mutators, unknown mutating path tools, and mutating or unclassified/non-allowlisted `bash` commands.
- [x] Add optional TUI status/check command entrypoint only after confirming the server/TUI split and bridge behavior for the current OpenCode version. Evidence: local docs `D:/uplift-labs/docs/08-programmatic-slash-commands.md` confirm `api.keymap.registerLayer({ commands })`; plugin TUI registers zero-LLM `autopilot.status` and `autopilot.check`; bundle smoke exercises them with a fake TUI API.
- [x] Add explicit TUI run/stop command behavior with prompt-mediated fallback unless a direct server-owned bridge is proven in tests or smoke output. Evidence: plugin TUI registers `autopilot.run` and `autopilot.stop`; source-equivalent smoke confirms dialog/toast fallback rather than claiming a direct server bridge.
- [x] Keep autonomous `run_next` event behavior disabled unless explicit config, runtime ownership, lock, cooldown, progress-backed loop-guard evidence, and no-blocker/no-MR prerequisites are all present. Evidence: classifier tests cover disabled defaults, missing active-run evidence, unknown sessions, blockers, MR wait, invalid locks, no-progress/wait/blocker/unknown prior outputs, and allowed owned autonomous run-next only under explicit config and progress-backed runtime evidence; source-equivalent smoke covers execute-time stale-runtime suppression.

## Documentation And Routing

- [x] Update README Autopilot installation/runtime guidance with trigger modes, default safety policy, and restart requirements for plugin/TUI changes. Evidence: README Manual Plugins documents trigger modes, passive safety, controlled ownership, autonomous opt-in, TUI commands, protected-path guard, bundle dependencies, and restart requirements.
- [x] Update README Routing Map to distinguish explicit `/autopilot`, TUI zero-LLM status/check commands, passive observe-mode triggers, controlled runtime triggers, and autonomous opt-in behavior. Evidence: README Routing Map now has separate bullets for agent-oriented `/autopilot`, programmatic server triggers, TUI commands, and protected-path guard.
- [x] Update `openspec-autopilot` skill guidance so agents understand event-triggered status/check/collect outputs and do not repeat no-progress calls. Evidence: `.opencode/skills/openspec-autopilot/SKILL.md` includes a Programmatic Trigger Hooks section with trigger modes, event-triggered output interpretation, no-progress loop guidance, TUI boundaries, and restart guidance.
- [x] Update `/autopilot` command wording only if explicit command behavior changes or needs to reference programmatic trigger boundaries. Evidence: no prompt-flow `/autopilot` behavior changed; command template remains valid and `tools/test-autopilot-instruction-drift.ts` still passes current command contract checks.
- [x] Document protected-path guard behavior and how plugin-owned controller paths remain the only allowed protected-state writer. Evidence: README Manual Plugins/Routing Map and `openspec-autopilot` skill document `tool.execute.before`, `.autopilot/**`, `openspec/changes/*/automation/**`, and plugin-owned controller writer boundaries.
- [x] Review relevant artifact frontmatter and command descriptions for discoverability if TUI commands or new plugin entrypoints are added. Evidence: skill frontmatter remains scoped to Autopilot, command description remains prompt-flow specific, plugin id remains `openspec.autopilot`, and bundle/instruction drift tests pass.

## Review Gates

- [x] Run `code-quality-reviewer` for the controller extraction, scheduler, plugin hooks, and protected-path guard. Evidence: final `code-quality-reviewer` pass returned clean/no blocking findings after runtime-output refresh, shell-like command guard coverage, scheduler dueAt/failure isolation, and file-size extraction fixes.
- [x] Run `test-coverage-reviewer` for event classification, scheduler behavior, runtime ownership gates, and negative safety cases. Evidence: final `test-coverage-reviewer` pass returned clean/no blocking findings; residual live TUI restart-loader E2E remains documented as source-equivalent risk.
- [x] Run `instruction-artifact-reviewer` after README, skill, command, or TUI command wording changes. Evidence: final `instruction-artifact-reviewer` pass returned clean/no blocking findings and confirmed README/skill permission status-only wording, protected-path contract, and TUI fallback consistency.
- [x] Run `deployment-config-reviewer` if trigger options or plugin packaging/config shape changes materially. Evidence: skipped as not materially applicable beyond documented nested plugin trigger options; `npm run validate` and instruction drift/bundle smoke cover packaging/config discoverability.
- [x] Run `openspec-consistency-review` before archive because this changes Autopilot/OpenCode lifecycle routing. Evidence: final OpenSpec architecture/consistency reviewer pass returned clean for implementation; only archive-only retrospective tasks remained.

## Validation

- [x] `npm run validate`. Evidence: passed with existing warning only: `OpenCode permission config uses top-level allow` in `opencode.json`.
- [x] `npm test`. Evidence: passed after adding `tools/test-autopilot-protected-path-guard.ts`; includes trigger classifier, scheduler, bundle smoke, instruction drift, protected guard, contract, OpenSpec output, evidence, check, and pre-push tests.
- [x] `npm run openspec:validate`. Evidence: passed, 15 OpenSpec items valid.
- [x] `openspec validate --all`. Evidence: covered by `npm run openspec:validate`, passed with 15 items valid.
- [x] `node tools/test-autopilot-bundle-smoke.ts` or updated bundle smoke covering new plugin hook surfaces. Evidence: passed; bundle smoke now covers source-equivalent event, before/after hooks, stale runtime revalidation, protected guard, worker markers, workspace/worktree stops, autonomous scoped run-next, and TUI fallback/status/check commands.
- [x] `npm run autopilot:validate -- <task-ledger.json>` for any new or modified Autopilot ledger fixtures, or record not-applicable when no ledger fixtures changed. Evidence: `npm run autopilot:validate -- openspec/changes/add-autopilot-programmatic-triggers/automation/task.json` passed.
- [x] Scoped Autopilot checkpoint. Evidence: `npm run autopilot:check -- --level standard --change add-autopilot-programmatic-triggers` passed with warning-only freshness `unknown` advisory and no blocking failures.
- [x] Manual/source-equivalent smoke: emit fake supported `file.watcher.updated` events and confirm one debounced status/check job is logged without `autopilot_run_next`. Evidence: `tools/test-autopilot-bundle-smoke.ts` passed source-equivalent file event smoke.
- [x] Manual/source-equivalent smoke: emit plugin-owned worker idle/report events and confirm exactly one scoped collect job is scheduled. Evidence: `tools/test-autopilot-bundle-smoke.ts` passed worker idle, report marker, consumed report, and stale-runtime smoke cases.
- [x] Manual TUI smoke after restart if TUI commands are implemented: command appears, status/check runs without LLM turn, scoped run uses dialog or prompt-mediated fallback. Evidence: source-equivalent TUI smoke passed with fake TUI API; live restart/loader E2E was not run in this non-interactive session and remains documented as residual risk in README.

## Acceptance Criteria

- [x] Existing `autopilot_*` tool output behavior remains compatible with current contract tests. Evidence: `node tools/test-autopilot-contract.ts`, `npm test`, and `node tools/test-autopilot-bundle-smoke.ts` passed after controller extraction and README bundle dependency sync.
- [x] Passive events can refresh status or cheap checks but cannot start claim-capable work by default. Evidence: classifier and bundle smoke tests passed; passive file/tool events schedule status/check only and never `run_next`.
- [x] Controlled collect/blocker/permission/workspace triggers require plugin-owned runtime evidence and ignore unrelated OpenCode events. Evidence: classifier and bundle smoke tests passed for owned/unknown worker, blocker, permission, workspace, and worktree events plus execute-time stale-runtime revalidation.
- [x] Scheduler prevents event storms, duplicate in-flight jobs, and recursion from Autopilot-generated events. Evidence: `tools/test-autopilot-trigger-scheduler.ts` passed debounce/coalescing, cooldown, single-flight, dispose, ordered due jobs, failure isolation, and recursion guard tests.
- [x] Protected Autopilot paths cannot be mutated directly by model-facing write/shell tools outside plugin-owned controller paths. Evidence: protected guard unit tests and plugin before-hook smoke passed for `.autopilot/**` and `openspec/changes/*/automation/**` across patch/edit/write/Serena/unknown mutating/bash surfaces.
- [x] TUI status/check commands, if implemented, run without an LLM turn and clearly report results or fallback behavior. Evidence: source-equivalent TUI smoke passed for status/check toasts and run/stop prompt-mediated fallback; live restart/loader E2E not run.
- [x] Autonomous `run_next` from events is disabled by default and guarded by explicit config plus runtime ownership evidence when enabled. Evidence: classifier and bundle smoke tests passed for disabled defaults, blockers/MR/locks/no-progress suppression, scoped run-next inspection, and stale runtime suppression before execution.

## Retrospective Before Archive

- [x] Review the completed change context, validation, reviewer gates, blockers, repeated work, wait time, and token-heavy steps. Evidence: `retrospective.md` records validation, reviewer waves, fixed material findings, and residual live-loader risk.
- [x] Write `retrospective.md` with evidence, problems, improvements, and archive gate decision. Evidence: `openspec/changes/add-autopilot-programmatic-triggers/retrospective.md` created with `Archive Gate Decision: passed`.
- [x] Create or update project-local OpenSpec follow-up changes for project-local findings. Evidence: no actionable project-local follow-up findings remained; `npm run openspec:retro-followups -- add-autopilot-programmatic-triggers` reported `changes: []` and `retrospectiveUpdated: false`.
- [x] For reusable findings, create or update `opencode-dev-kit` OpenSpec proposals/changes only when the current repository owns them; otherwise record a local handoff and do not write cross-repo without explicit approval. Evidence: no reusable follow-up findings remained after in-scope fixes; retrospective outputs record none.
- [x] Run `npm run openspec:retro-followups -- add-autopilot-programmatic-triggers` when available so actionable retrospective findings create or update OpenSpec follow-up changes before archive. Evidence: command passed with no generated changes.
- [x] Confirm archive is allowed only after the retro gate passes or an approved skip reason is recorded. Evidence: `npm run openspec:retro-gate -- add-autopilot-programmatic-triggers` passed with `archiveAllowed: true`.
